<Window x:Class="Interfata.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vinyl Vibes" Height="450" Width="600"
        Background="#1A1A2E">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Colectie Vinyl Vibes" 
               Foreground="White" FontSize="20" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" x:Name="lstVinyluri" Background="#16213E" Foreground="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <TextBlock Text="{Binding DisplayNume}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding DisplayDetalii}" FontSize="12" Foreground="LightGray"/>
                        <TextBlock Text="{Binding DisplayStare}" FontSize="11" Foreground="Pink"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>